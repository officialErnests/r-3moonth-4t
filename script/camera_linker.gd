extends WorldEnvironment

@export var camera: Camera3D
# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass
	# environment.background_camera_feed_id = camera.get_id()
